<h1>Tasks</h1>

<ul>
<% @tasks.each do |task| %>
<li><%= link_to task.title, task_path(task)%></li>
<p><%= link_to "Delete", task_path(task), data: {turbo_method: :delete, turbo_confirm: "R U sure?"} %></p>
<% task.completed %>

<% end %>
</ul>
